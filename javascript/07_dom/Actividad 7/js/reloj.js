const clock = document.getElementById("reloj");
export default function digitalClock (clock, btnPlay, btnStop) {
    let clockTempo = clock;
    clock.addEventListener("click",console.log(clockTempo));
}

